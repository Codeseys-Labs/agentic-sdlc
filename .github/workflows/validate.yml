name: validate-bundle

on:
  push:
    branches: [main]
  pull_request:

jobs:
  check:
    name: mise check (${{ matrix.os }})
    strategy:
      fail-fast: false
      matrix:
        os:
          - blacksmith-2vcpu-ubuntu-2404
          - blacksmith-6vcpu-macos-latest
          - blacksmith-2vcpu-windows-2025
    runs-on: ${{ matrix.os }}
    steps:
      # actions/checkout v4.3.1
      - uses: actions/checkout@34e114876b0b11c390a56381ad16ebd13914f8d5
      # jdx/mise-action v2.4.7
      - uses: jdx/mise-action@c37c93293d6b742fc901e1406b8f764f6fb19dac
        with:
          # The exact maintenance mise, not the action's default latest: the canonical
          # lock-regeneration test in tests/test_gate_graph.py skips on any other version, so an
          # unpinned mise-action leaves that contract gated on no runner at all (agentic-sdlc-c7e2).
          # This pins only the mise CLI; the toolchain stays mise.lock's.
          version: "2026.4.27"
          install: true
      # advisory forensics probe for seed agentic-sdlc-df5f (mechanisms A/B1/B2); remove once the runner's btime/ctime data is recorded
      - name: Advisory host capability probe (never a gate leaf)
        if: runner.os == 'Linux'
        continue-on-error: true
        timeout-minutes: 2
        run: mise exec -- python3 -u .github/scripts/ci-capability-probe.py || true
      - name: Run authoritative check
        run: mise run check
