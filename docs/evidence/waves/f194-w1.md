# Wave `f194-w1` — evidence

Staged by workstream WS-A (`ws-f194-journey`) with its executed journey record under
`docs/evidence/waves/f194-w1/` (see `journey.md` there). Fields only the conductor can know at
fan-in — dispositions, reviewer identity, the operator approval, the integration commit, and the
merged-head gate receipt — are written `unknown` rather than guessed, per this template's own rule,
and the conductor fills them at integration.

## Nodes

| node | role | model_id | disposition | commit | reviewed-by |
|---|---|---|---|---|---|
| `ws-f194-journey` | implementer | `global.anthropic.claude-fable-5[1m]` | unknown | unknown | unknown |
| `ws-release-validity` | implementer | unknown | unknown | unknown | unknown |
| `ws-f194-review` | reviewer | unknown | unknown | — | — |

`ws-f194-journey`'s `model_id` is the exact id its harness reports for the running agent; no
independent adapter readback beyond the harness statement is available to the node itself, and
that source is stated rather than upgraded. Its `commit` is `unknown` because this workstream's
dispatch reserves all git mutation to the conductor: the worktree
(`work/agentic-sdlc-f194-journey`) holds only the new evidence files, uncommitted. Its
`disposition` is `unknown` because a row's disposition belongs to its reviewer, and a row reviewed
by its author is unreviewed. `ws-release-validity` is WS-B per the wave frame; its row is the
conductor's to complete from that workstream's own record.

## Outcome

- `outcome`: `accepted`
- `recorded`: 2026-08-24, by the conducting session, after the independent review (a distinct
  read-only reviewer agent; recommendation "accept for fan-in with named edits", zero blocking
  findings, ~25 internal and 2 external evidence cross-checks) and with that review's named edits
  applied in this same integration. The reviewer's vocabulary ruling is adopted as the record's
  reasoning: every journey step reached its named success or named refusal in one contiguous run;
  the two earlier aborted attempts were pre-effect and are disclosed; nothing carries unknown
  effect; `remediation-progress` would be false because this wave remediates nothing. The one
  product defect surfaced (journey.md FINDING-1, post-uninstall readers report
  `degraded`/`owned-entry-conflict` against a completed receipted retirement) is filed as seed
  `agentic-sdlc-42ec`, not fixed here, and does not unmake any step's named success.
- `merged-head gate`: run via `scripts/gate_receipt.py record --gate "mise run check"` on the
  integrated head; its receipt is committed as
  `docs/evidence/waves/f194-w1/gate-receipt-merged-head.json` in the wave-close change. This
  record lands only behind that gate's green verdict, so the record cannot land with the gate
  failing.

## Operator approval

> unknown — the dated verbatim approval is recorded here by the conductor at fan-in; it must
> precede the committer date below. No approval existed when this file was staged, and nothing in
> this workstream's record substitutes for one.

## Integration

- Integration commit: unknown — base `main` (`6c30728677c4e122bf294a887c62fbb63a1b05c5`),
  branch `work/agentic-sdlc-f194-journey`
- Committer date: unknown

## Gate receipt

Recorded on the MERGED head with
`python scripts/gate_receipt.py record --gate "mise run check" --out <path> -- mise run check`. A
worktree-head receipt is not evidence about the merged tree.

- `self_digest`: unknown
- `outcome`: unknown
- `head.commit`: unknown

Everything above is evidence. None of it authorizes push, publication, PR mutation, merge,
deployment, or any other outward effect; each needs explicit operation-specific authorization.
