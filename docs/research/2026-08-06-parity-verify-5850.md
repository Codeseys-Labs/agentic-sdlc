## Verdict summary

I re-read every cited primary file and **executed** the load-bearing artifacts on both sides (`python3 -m unittest` on 4 agentic-sdlc suites, `npx tsx --test` on 3 fractal-workflows suites, `bun` on pi-lab's SDLC extension, plus planted-violation probes against two validators). 8 claims confirmed, **6 refuted**, 3 partly refuted.

---

## THE SINGLE MOST IMPORTANT ERROR

**The analyst compared against the wrong sibling repo, and it inverts the three worst rows plus the #1 recommended fix.**

`<workspace>/pi-agentic-sdlc-skills/` is a fork of agentic-sdlc's *own* doctrine (`skills/agentic-sdlc-orchestrator/references/mission-loop.md` is **byte-identical** to agentic-sdlc's — `diff` exit 0), and it has an **executable enforcement layer for exactly the numbers the analyst declared unenforced everywhere**:

`extensions/sdlc-mission.ts:540`
```ts
export const PASS_BUDGETS: Record<string, number> = { global: 6, frame: 1, discover: 2, research: 2, plan: 2, act: 3 };
```

Those are character-for-character the integers in `commands/sdlc-mission.md:39-41` ("global passes ≤6; Frame ≤1, Discover/Research/Plan ≤2, Act ≤3"). `chargePass()` (`sdlc-mission.ts:571-593`) increments, **persists to `.sdlc/mission-<slug>.json` before the caller may delegate**, and refuses. I ran it:

```
BUDGETS {"global":6,"frame":1,"discover":2,"research":2,"plan":2,"act":3}
frame pass 1 allowed= true  | pass 1/1 for frame; global 1/6
frame pass 2 allowed= false | REFUSED: frame budget 1 exhausted (this would be pass 2)...
```

And the WIP caps are schema maximums, not prose — `schemas/frame.schema.json` `properties.caps`: `impl` max 3, `waveSize` max 3, `research` max 3, `integration` **`{"const": 1}`**, `critique` max 1, `nesting` max 2, `reviewRounds` max 2. Backed by a runtime fan-out cap (`chains/sdlc-wave.chain.json:30,42` `expand.maxItems: 3`) and a drift check that diffs the doctrine table against the schema — `bun extensions/sdlc-mission.ts` → `ok caps-agree [EXECUTED] 7 caps match frame.schema.json defaults exactly`, `ok fanout-capped:sdlc-wave.chain.json#4 [EXECUTED] maxItems=3`. Full selftest `RESULT: PASS`, exit 0.

**Consequences:** the "WIP caps" and "Backflow" rows are not "PI-LAB AHEAD via a fractal-workflows sibling on a different axis" — pi-lab is ahead **on the identical axis with the identical integers**. And gap #1's "smallest closing change" (port 598 lines of `frame-ledger.ts` from TS to Python) is the wrong target: the ~55-line `chargePass` + a JSON Schema, which already encode agentic-sdlc's own numbers, are a far smaller and exactly-shaped port. The harvest memo also missed this (`rg "chargePass|PASS_BUDGETS|frame.schema"` over `docs/research/2026-08-05-pi-lab-harvest-memo.md` → zero hits), so this is an unrecorded gap, not a known one.

---

## Per-claim adjudication

### REFUTED

**1. "WIP caps: PI-LAB AHEAD (via fractal-workflows sibling) — genuinely enforced numeric ceiling *on a different axis*"** → **REFUTED.** Same axis, same numbers. Evidence above. Corrected verdict: **PI-LAB AHEAD, same axis** — `pi-agentic-sdlc-skills/schemas/frame.schema.json` encodes all five caps as schema bounds (`integration` as `const: 1`), `chains/sdlc-wave.chain.json:30` enforces impl fan-out at runtime, `extensions/sdlc-mission.ts:495-510` fails on doctrine/schema drift. agentic-sdlc: **ABSENT as code** (confirmed — `rg -ln "WIP|wip_cap|delegation_depth|backflow" scripts/ skills/agentic-sdlc/tools/` exits 1).

**2. "Backflow: PI-LAB AHEAD — backed by the same enforced ledger (no dedicated API, budget mechanics cover it)"** → **REFUTED on mechanism.** pi-lab has a *dedicated, purpose-built* backflow counter: `PASS_BUDGETS` + `chargePass`, persisted, with `stopReason: "bound-tripped"` and the explicit comment "*This is the whole control: `loopUntilDry` cannot report why it stopped, so we count here*" (`sdlc-mission.ts:569`). Corrected verdict: **PI-LAB AHEAD, decisively — a dedicated executed pass-budget counter with a persisted state file, holding agentic-sdlc's exact integers.**

**3. "Mission/backlog-zero doctrine: pi-lab effectively ABSENT as a named doctrine document; AGENTIC-SDLC AHEAD"** → **REFUTED.** `pi-agentic-sdlc-skills/skills/agentic-sdlc-orchestrator/references/mission-loop.md` is byte-identical (109 lines, `diff` exit 0), including the 8-class taxonomy at line 25 and the priority formula at line 31. Direction of copy favours agentic-sdlc (`git log --diff-filter=A`: agentic-sdlc 2026-07-04 `084a373`; pi-agentic-sdlc-skills 2026-07-25 `7abc400`) — so agentic-sdlc is the *author*, but "pi-lab absent" is false. Corrected verdict: **agentic-sdlc originated it; pi-lab's identical copy is wired to enforcement (frame.schema.json caps, chargePass) that agentic-sdlc's original is not.** The end-to-end-blocker sub-claim (Seeds read-only) is CONFIRMED (`seeds-launcher.mjs:868-869` admits only `prime` / `ready` / `blocked` / `--version`; `tests/test_seeds_launcher.py:451` plants `"create","no"` and asserts refusal).

**4. "Model-tier routing: agentic-sdlc … no code resolves an actual model ID at dispatch time; both remain DESIGNED as an actual runtime gate"** → **REFUTED as stated.** `skills/model-tier-rightsizing/scripts/receipt_admission.py` (462 lines, stdlib-only, importable + CLI) is real executable code, not doc-lint. I planted violations:
```
GOOD errors: []
PLANTED resolution_state = inherited   -> ['resolution_state must equal resolved']
PLANTED resolution_state = unresolved  -> ['resolution_state must equal resolved']
PLANTED model_readback_status=unavailable -> ['model_readback_status must equal verified']
PLANTED resolved_model_id = gpt-4      -> ['resolved_model_id must match the immutable injected exact model ID', ...]
```
`echo '{}' | python3 …/receipt_admission.py` → `{"errors":["missing fields: …"],"status":"invalid"}`, exit 1. Exercised at 14 sites in `tests/test_runtime_contract_validation.py` (41 tests, pass). Corrected verdict: **agentic-sdlc SHIPPED as an executable receipt validator (`resolution_state` tri-state genuinely refuses in code); DESIGNED as a spawn gate** — `skills/model-tier-rightsizing/SKILL.md:107` is explicit and honest: *"This repository supplies no host launcher. An external authenticated harness is the sole admission and spawn authority."* This is the analyst's biggest under-grade of agentic-sdlc.

**5. "pi-lab's 9-role model+tool binding table has zero code consumers, zero tests, zero installer"** → **REFUTED on "zero code consumers."** `bun extensions/sdlc-mission.ts` executes `model-pinned:<role>`, `read-only:<role>`, and `no-recursion:<role>` over all 9 agents, all `[EXECUTED]` PASS (e.g. `ok model-pinned:sdlc-critic [EXECUTED] bedrock-mantle/openai.gpt-5.6-sol`). "Zero installer" is correct (`README.md:8` "No install step").

**6. "Fan-in authority: pi-lab DESIGNED … no pi-lab 'SDLC fan-in' doctrine document found; AGENTIC-SDLC AHEAD"** → **REFUTED.** `pi-agentic-sdlc-skills/schemas/integration.schema.json` requires `"pushed": {"const": false}` — the schema *cannot represent* a push, self-described as *"weaker than a sandbox and stronger than a sentence"* — plus `declinedOutwardOps` and `gateHashReasserted` as required fields, and the integrator is a single non-parallel chain step (structural n=1). Validated by the selftest (`ok schema:integration.schema.json [EXECUTED] requires 6 keys`). Corrected verdict: **PI-LAB AHEAD on the mechanism** (a structural refusal at the report boundary); agentic-sdlc ahead only on doctrine breadth. Neither blocks a real `git merge`.

### PARTLY REFUTED

**7. Gap #1's claim that the numbers are "doc-lint asserted"** → the reality is *worse* than stated, in agentic-sdlc's disfavour. `tests/test_deep_work_loop.py:179-181` does **not** assert the number 2 — it regexes `delegation.{0,80}(?:cap|bound)` and `"no unbounded recursive delegation"`. `rg -c "≤" tests/*.py` returns **nothing**. No test anywhere asserts ≤3/≤2/≤6. Corrected: those integers have **zero** coverage, not doc-lint coverage.

**8. "`references/tiered-orchestration.md:62-67` … name numeric budgets"** → **REFUTED.** Lines 62-67 contain no integers: *"respects a global pass ceiling, per-phase re-entry budgets, and a resource floor."* Worse — and the analyst missed this — `deep-work-loop.md:220-222` routes to it as the owning site: *"respects the global pass ceiling and per-phase re-entry budgets defined in `references/tiered-orchestration.md`"*, but the numbers live only in `commands/sdlc-mission.md:39-41`. **The single-authoritative-site pointer dangles**, which is precisely the "fork a second copy that drifts" failure `deep-work-loop.md:9` claims to prevent.

**9. Loop-shape row's source characterization** → `pi-lab/skills/house/loop-design/SKILL.md` contains no "5 failure classes" and no "review-mode/write-mode" taxonomy (`rg -in "failure class|five |review mode|write mode"` → nothing but the frontmatter description). Its own frontmatter (line 9) says *"No loop described here is installed by pi-lab. This skill grants no permission and enforces nothing."* And `SOURCE.md:24` credits *"ECC only as the place pi-lab observed them"* (lineage is Wiener/Anatoli/Osmani). So "ECC prescribes a **checkable** loop shape" is a paraphrase into a requirement neither source made. The PARITY verdict itself stands.

### CONFIRMED

- **Loop phase shape / stop conditions** — `deep-work-loop.md:14` has the exact 7-phase string; `:178` reads *"(adapted from pi-lab's loop-design skill)"*. Both prose-only. PARITY holds. `tests/test_deep_work_loop.py` (11 tests, pass) is doc-lint, correctly graded.
- **Budget ledger, pi-lab side** — `frame-ledger.ts` is 598 lines; `createChildLedger` line 290 `grant: Math.min(clampGrant(init.grant), parent.grant)`; `windDownReserveThreshold`/`isWindingDown` at 493/507; `RED GUARD: nothing outside shrinkGrant() assigns to a ledger grant` (`frame-ledger-unit.test.ts:566`). Executed: 28 tests pass (24 unit + 4 wiring, not "≈24+").
- **Depth cap, pi-lab side** — `config.ts:50,57` (`DEFAULT_MAX_WORKFLOW_DEPTH = 3`, `ABSOLUTE_MAX_WORKFLOW_DEPTH = 5`), `workflow-tool.ts:39` names `DEPTH_CAP_EXCEEDED`/`WORKFLOW_CYCLE_DETECTED` "neither is retryable", contract quote verbatim at `workflow-capability-contract.ts:349`. Executed: 18/18 pass, including `K: a request for depth 99 is CLAMPED` and `M (rule 5): a script cannot raise the depth cap at runtime`.
- **Fail-closed worktree isolation** — pi-lab's suite is real and runs: 9/9 pass in 12.7s against `runWorkflow` with a recording runner, asserting typed refusal + zero dispatch + no orphaned branch. agentic-sdlc's `worktree-failclosed-tests.md` is 227 lines and self-labels at line 7: *"nothing here is executable — it is a specification a future implementer turns into real Python tests."* `fd 'worktree' tests/` → nothing. Verdict PI-LAB AHEAD holds. **Correction: 9 test cases, not 18** (18 is `fractal-depth.test.ts`); 374 lines is right.
- **Worktree wave pattern PARITY** — all four rules verbatim in `seeds-worktrees.md:125,132,145,163`; `cp -al` hardlink at :154. pi-lab's `git-hygiene/SKILL.md` (99 lines) matches at :21,:28,:60,:78.
- **Section (2), snapshot-critique invention** — `mission-loop.md:61-69` confirmed, but note pi-lab ships it as a *role* with a pinned model and read-only toolset (`agents/sdlc-critic.md:3,41`, selftest-verified `read-only:sdlc-critic [EXECUTED]`) — more than a paragraph, though agentic-sdlc authored the text first.
- **Section (3) instances** — all three quotes verbatim at `sdlc-mission.md:31`, `:39-41`, `mission-loop.md:56`. The asymmetry claim is right, and stronger than stated (no test asserts any of the integers).

### Line-reference nits
`tests/test_authority_corrections.py:56,82` and `test_deep_work_loop.py:126,179` point at `def` lines, not assertions; substance checks out one to three lines below in each case.